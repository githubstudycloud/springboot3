<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.framework.excel.mapper.ExcelTemplateConfigMapper">

    <resultMap id="BaseResultMap" type="com.framework.excel.entity.config.ExcelTemplateConfig">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="template_key" property="templateKey" jdbcType="VARCHAR"/>
        <result column="template_name" property="templateName" jdbcType="VARCHAR"/>
        <result column="entity_class" property="entityClass" jdbcType="VARCHAR"/>
        <result column="table_name" property="tableName" jdbcType="VARCHAR"/>
        <result column="sheet_name" property="sheetName" jdbcType="VARCHAR"/>
        <result column="primary_key_fields" property="primaryKeyFields" jdbcType="VARCHAR"/>
        <result column="update_mode" property="updateMode" jdbcType="VARCHAR"/>
        <result column="business_config" property="businessConfig" jdbcType="LONGVARCHAR"/>
        <result column="description" property="description" jdbcType="VARCHAR"/>
        <result column="enabled" property="enabled" jdbcType="BOOLEAN"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, template_key, template_name, entity_class, table_name, sheet_name, 
        primary_key_fields, update_mode, business_config, description, enabled, 
        create_time, update_time
    </sql>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM excel_template_config
        WHERE id = #{id,jdbcType=BIGINT}
    </select>

    <select id="selectByTemplateKey" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM excel_template_config
        WHERE template_key = #{templateKey,jdbcType=VARCHAR}
    </select>

    <select id="selectEnabledList" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM excel_template_config
        WHERE enabled = 1
        ORDER BY template_key
    </select>

    <insert id="insert" parameterType="com.framework.excel.entity.config.ExcelTemplateConfig" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO excel_template_config (
            template_key, template_name, entity_class, table_name, sheet_name,
            primary_key_fields, update_mode, business_config, description, enabled,
            create_time, update_time
        ) VALUES (
            #{templateKey,jdbcType=VARCHAR}, #{templateName,jdbcType=VARCHAR}, 
            #{entityClass,jdbcType=VARCHAR}, #{tableName,jdbcType=VARCHAR}, 
            #{sheetName,jdbcType=VARCHAR}, #{primaryKeyFields,jdbcType=VARCHAR}, 
            #{updateMode,jdbcType=VARCHAR}, #{businessConfig,jdbcType=LONGVARCHAR}, 
            #{description,jdbcType=VARCHAR}, #{enabled,jdbcType=BOOLEAN},
            NOW(), NOW()
        )
    </insert>

    <update id="updateById" parameterType="com.framework.excel.entity.config.ExcelTemplateConfig">
        UPDATE excel_template_config
        SET template_name = #{templateName,jdbcType=VARCHAR},
            entity_class = #{entityClass,jdbcType=VARCHAR},
            table_name = #{tableName,jdbcType=VARCHAR},
            sheet_name = #{sheetName,jdbcType=VARCHAR},
            primary_key_fields = #{primaryKeyFields,jdbcType=VARCHAR},
            update_mode = #{updateMode,jdbcType=VARCHAR},
            business_config = #{businessConfig,jdbcType=LONGVARCHAR},
            description = #{description,jdbcType=VARCHAR},
            enabled = #{enabled,jdbcType=BOOLEAN},
            update_time = NOW()
        WHERE id = #{id,jdbcType=BIGINT}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM excel_template_config
        WHERE id = #{id,jdbcType=BIGINT}
    </delete>

</mapper> 