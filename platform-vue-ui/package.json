{
  "name": "platform-vue-ui-parent",
  "version": "1.0.0",
  "private": true,
  "description": "Platform Vue UI Parent Project",
  "author": "Your Name",
  "license": "MIT",
  "workspaces": [
    "platform-vue-ui-common",
    "platform-vue-ui-app"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=platform-vue-ui-app",
    "build": "npm run build --workspace=platform-vue-ui-app",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces",
    "clean": "rm -rf node_modules && npm run clean --workspaces"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "devDependencies": {
    "prettier": "^3.2.2",
    "rimraf": "^5.0.5"
  }
}
