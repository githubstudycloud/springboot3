#!/bin/bash
# 为shell脚本添加执行权限

echo "正在为shell脚本添加执行权限..."
chmod +x test-dependencies.sh
chmod +x run-ddd-demo.sh
chmod +x setup-permissions.sh

echo "已添加执行权限。现在可以直接运行脚本了。"
echo "例如: ./test-dependencies.sh"
